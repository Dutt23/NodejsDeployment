{{#if error}}
<div class="alert alert-danger">{{error}}</div>
{{/if}}
{{#each errors}}
<div class="alert alert-danger">{{text}}</div>
{{!-- If no errors then whatever is in here whill happen --}} 
{{else}} 
{{/each}}